n = int(input())
h = list(map(int, input().split()))
ans = 1
if n <= 1:
    print(n)
for d in range(1, n):
    dp = [1] * n
    for i in range(d, n):
        if h[i] > h[i - d]:
            dp[i] = dp[i - d] + 1
            ans = max(ans, dp[i])
print(ans)

"""
========================================
  第六题：园艺 算法详解
========================================

【题目】
有 n 棵树排成一排，第 i 棵树的高度为 hᵢ，相邻树的间隔相同。
砍掉一些树后，要求：
  1. 剩下的树等间隔分布（相邻两棵的位置差固定）
  2. 从左到右高度严格递升（左边 < 右边）

问：最多能留下多少棵树？


【题目理解（生活化比喻）】

想象一排树：
  位置:   1   2   3   4   5   6
  高度:   3   5   4   7   6   7

规则举例：
  d=1（挨着留）：位置1→2→3→4... 要求每对相邻的树左<右
  d=2（隔1留1）：位置1→3→5 或 2→4→6，要求同上
  d=3（隔2留1）：位置1→4 或 2→5 或 3→6

对于每种间距d，找出最长的递增链，再取所有d中的最大值。


【算法核心：枚举间距 + 动态规划】

整体策略分两步：
  第一步：暴力枚举所有可能的间距 d（从1到n-1）
  第二步：对每个固定的d，用DP找最长递增等间距子序列

为什么需要枚举d？
  因为我们不知道最优解用的是什么间距，
  所以每种间距都要试一遍，取最大值。


【代码逐行解析】

第1行：读取树的总数
  n = int(input())

第2行：读取每棵树的高度，存入列表
  h = list(map(int, input().split()))

第3行：初始化答案为1（至少能留1棵树）
  ans = 1

第4~5行：特殊情况处理
  if n <= 1:
      print(n)
  只有0或1棵树时，直接输出数量即可。

第6行：外层循环——枚举所有间距 d
  for d in range(1, n):
  d 的含义：相邻两棵留下来的树之间隔了几棵被砍掉的树
  例如 d=2 表示"隔1棵留1棵"，即位置 0,2,4,...

第7行：内层循环前——初始化dp数组
  dp = [1] * n
  dp[i] 表示：以第i棵树为最后一棵、间距为d时，
            最长的严格递增序列长度。
  初始值为1，因为每棵树自己单独就算长度1。

第8~11行：内层循环——动态规划递推
  for i in range(d, n):
      # 为什么从d开始？因为位置 < d 的树没有「前一棵」(i-d会<0)

      if h[i] > h[i - d]:        # 当前树比它前面那棵高吗？
          dp[i] = dp[i - d] + 1  # 能接上！在前一棵的基础上+1
          ans = max(ans, dp[i])  # 更新全局最大值
      # 如果不满足 h[i] > h[i-d]，则dp[i]保持为1（断开，重新开始）


【手动模拟示例】

输入：n=6, h=[3, 5, 4, 7, 6, 7]

═════════════ d=1（相邻留） ═════════════

  i=1: h[1]=5 > h[0]=3 ✓ → dp[1]=dp[0]+1=2, ans=2
  i=2: h[2]=4 > h[1]=5 ✗ → dp[2]=1
  i=3: h[3]=7 > h[2]=4 ✓ → dp[3]=dp[2]+1=2, ans=2
  i=4: h[4]=6 > h[3]=7 ✗ → dp[4]=1
  i=5: h[5]=7 > h[4]=6 ✓ → dp[5]=dp[4]+1=2, ans=2
  d=1 最大值: 2

  可视化：3→5✓ → ✗(断) → 4→7✓ → ✗(断) → 6→7✓
           最长段: 3→5(2棵) 或 4→7(2棵) 或 6→7(2棵)

═════════════ d=2（隔1留1） ═════════════

  分成两条独立链条：
  链A(偶数位): 位置0→2→4
  链B(奇数位): 位置1→3→5

  链A:
  i=2: h[2]=4 > h[0]=3 ✓ → dp[2]=dp[0]+1=2, ans=2
  i=4: h[4]=6 > h[2]=4 ✓ → dp[4]=dp[2]+1=3, ans=3 ★

  链B:
  i=3: h[3]=7 > h[1]=5 ✓ → dp[3]=dp[1]+1=2, ans=max(3,2)=3
  i=5: h[5]=7 > h[3]=7 ✗ → dp[5]=1

  d=2 最大值: 3 ← 这就是最终答案！

  可视化:
    链A: 位置1(3) → 位置3(4) → 位置5(6)  长度=3 🎉
    链B: 位置2(5) → 位置4(7) → 位置6(7)  长度=2 (7不>7)

═════════════ d=3（隔2留1） ═════════════

  三条短链:
  i=3: h[3]=7 > h[0]=3 ✓ → dp[3]=2
  i=4: h[4]=6 > h[1]=5 ✓ → dp[4]=2
  i=5: h[5]=7 > h[2]=4 ✓ → dp[5]=2
  d=3 最大值: 2

═════════════ 最终结果 ═════════════

  ans = max(d=1时的2, d=2时的3, d=3时的2, ...) = 3 ✅


【关键概念解释】

Q: 为什么 for i in range(d, n) 要从 d 开始？
A: 因为要访问 h[i-d]。如果 i<d，则 i-d<0，越界了！
   第d棵树（索引d）的前一棵是第0棵（索引0），这是合法的。
   而第0~d-1棵树没有前驱，所以它们只能作为链的起点(dp=1)。

Q: dp 数组为什么每次外层循环都要重新初始化？
A: 因为不同的 d 对应完全不同的「邻接关系」。
   d=1 时位置2的前驱是位置1，
   d=2 时位置2的前驱是位置0，
   两者毫无关系，所以必须重置。


【算法特点】

时间复杂度：O(n²)
  外层循环 d 有 O(n) 种取值
  内层循环 i 有 O(n) 次迭代
  总计 O(n²)，对于蓝桥杯的数据范围(n≤5000)完全够用

空间复杂度：O(n)
  仅使用一个长度为 n 的 dp 数组

适用场景：等距约束下的最长递增子序列问题，
          核心技巧是「固定步长 + DP递推」。


【一句话总结】
枚举所有可能间距d → 对每个d用dp找最长递增等间距链 → 取所有d中的最大值即为答案
"""