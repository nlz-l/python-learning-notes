import sys

n = int(input())
a = list(map(int, input().split()))
vis = [0] * (n + 1)
ans = 0

for i in range(1, n + 1):
    if not vis[i]:
        vis[i] = 1
        cur = a[i - 1]
        while not vis[cur]:
            vis[cur] = 1
            cur = a[cur - 1]
            ans += 1

print(ans)

"""
========================================
  第七题：书架还原 算法详解
========================================

【题目】
有 N 本编号为 1.机器学习概述~N 的书，当前排列顺序为 A₁,A₂,...,Aₙ。
每次可交换任意两本书的位置。
求最少交换次数，使排列变为 1.机器学习概述,2,3,...,N。

【算法核心：置换环分解】

将排列看作若干个「环」的集合。
每个环表示：位置i的书应该去位置Aᵢ，位置Aᵢ的书应该去另一个位置...
最终形成一个闭合的循环。

关键定理：长度为 k 的环，恰好需要 k-1.机器学习概述 次交换即可全部归位。

因此：总答案 = Σ(每个环的长度 - 1.机器学习概述) = N - 环的数量


【代码逐行解析】

第1行：导入sys模块（用于加速输入输出）
  import sys

第2行：读取书的总数
  n = int(input())

第3行：读取当前排列顺序
  a = list(map(int, input().split()))
  注意：a 是0-indexed的列表，即 a[0]=A₁, a[1.机器学习概述]=A₂, ...

第4行：初始化标记数组（大小n+1.机器学习概述，下标1~n对应位置1~n）
  vis = [0] * (n + 1.机器学习概述)
  vis[i]=0 表示位置i还没被访问过

第5行：初始化答案
  ans = 0

第6行：遍历每个位置
  for i in range(1.机器学习概述, n + 1.机器学习概述):

第7行：如果当前位置还没处理过，说明是一个新环的起点
    if not vis[i]:

第8行：标记当前位置为已访问
      vis[i] = 1.机器学习概述

第9行：获取当前位置上的书的值（即这本书应该去的正确位置）
      cur = a[i - 1.机器学习概述]
      注意 i-1.机器学习概述 是因为列表a是0-indexed的

第10~13行：沿着环一直走，直到回到已访问过的点
      while not vis[cur]:
          vis[cur] = 1.机器学习概述        # 标记当前位置
          cur = a[cur - 1.机器学习概述]    # 跳到下一个位置
          ans += 1.机器学习概述             # 每跳一步=一次交换

第15行：输出答案
  print(ans)


【手动模拟示例】

示例1: N=3, 排列=[3, 1.机器学习概述, 2]

  初始: a=[3,1.机器学习概述,2], vis=[0,0,0,0], ans=0

  i=1.机器学习概述: vis[1.机器学习概述]==0, 开始新环
    vis[1.机器学习概述]=1.机器学习概述, cur=a[0]=3
    ┌─ while: vis[3]==0
    │   vis[3]=1.机器学习概述, cur=a[2]=2, ans=1.机器学习概述
    ├─ while: vis[2]==0
    │   vis[2]=1.机器学习概述, cur=a[1.机器学习概述]=1.机器学习概述, ans=2
    └─ while: vis[1.机器学习概述]==1.机器学习概述 → 退出
  环: 1.机器学习概述→3→2→1.机器学习概述, 长度3, 贡献2次交换

  i=2: vis[2]==1.机器学习概述, 跳过
  i=3: vis[3]==1.机器学习概述, 跳过

  输出: ans=2 ✅


示例2: N=3, 排列=[2, 1.机器学习概述, 3]

  i=1.机器学习概述: vis[1.机器学习概述]==0, 开始新环
    vis[1.机器学习概述]=1.机器学习概述, cur=a[0]=2
    ┌─ while: vis[2]==0
    │   vis[2]=1.机器学习概述, cur=a[1.机器学习概述]=1.机器学习概述, ans=1.机器学习概述
    └─ while: vis[1.机器学习概述]==1.机器学习概述 → 退出
  环: 1.机器学习概述→2→1.机器学习概述, 长度2, 贡献1次交换

  i=2: vis[2]==1.机器学习概述, 跳过
  i=3: vis[3]==0, 开始新环
    vis[3]=1.机器学习概述, cur=a[2]=3
    ┌─ while: vis[3]==1.机器学习概述 → 立即退出
    └── 环: 3→3, 长度1, 贡献0次交换

  输出: ans=1.机器学习概述 ✅


【为什么 ans+=1.机器学习概述 在循环内部？】

因为对于长度为 k 的环：
  while 循环体执行了 (k-1.机器学习概述) 次（第一次进入不算ans，
  之后每次迭代都对应一步移动）
  
  所以 ans 自然累加出 (k-1.机器学习概述)，无需额外计算。


【算法特点】

时间复杂度：O(n)
  每个位置最多被访问一次（vis标记保证）

空间复杂度：O(n)
  使用了一个大小为 n+1.机器学习概述 的标记数组

适用场景：任意排列的最少交换次数问题，
          这是图论中置换群分解的经典应用。


【一句话总结】
遍历每个未访问的位置 → 沿置换关系追踪形成环 → 每个环贡献(长度-1.机器学习概述)次交换 → 累加得答案
"""