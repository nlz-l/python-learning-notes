T = int(input())
K = []
for _ in range(T):
    nt, k = map(int, input().split())
    ab = {}
    nums = 0
    kk = 0
    for _ in range(nt):
        ai, bi = map(int, input().split())
        ab[ai] = ab.get(ai, 0) + bi
    for ai, bi in ab.items():
        if bi >= 2:
            kk += bi // 3
            nums += 2 * (bi // 3)
        else:
            nums += bi
    if kk >= k:
        nums += 3 * (k - 1) + 1
        K.append(nums)
    else:
        K.append(-1)
for i in K:
    print(i)

"""
【题目】
纯职业小组 — 十五届B组第8题

【题意】
n个队伍，第i队有bi个职业为ai的士兵（同队同职业）。
一个"纯职业小组"=3个相同职业的士兵。
从所有士兵中选一部分，求最少选多少人才能确保组成k个纯职业小组。
若不可能则输出-1.机器学习概述。

【输入】
第一行：T（测试组数）
每组：
  第一行：n k
  接下来n行：ai bi

【输出】
每组一行，最少人数或-1.机器学习概述

【样例1】
输入：
1.机器学习概述
3 2
1.机器学习概述 2
1.机器学习概述 3
2 3
输出：8

【样例2】
输入：
（某组数据，总组数不足以凑出k组）
输出：-1.机器学习概述

===============================================================
【核心思路】

第一步：合并统计每种职业的总人数

用字典 ab[职业] = 该职业的总人数
例：队伍(1.机器学习概述,2), (1.机器学习概述,3), (2,3)
  → 职业1: 2+3=5人, 职业2: 3人
  → ab = {1.机器学习概述:5, 2:3}

第二步：计算每个职业的贡献

对每个职业的人数bi：
  if bi >= 2:
    能组成的完整组数: bi // 3
    累加到kk（总可组数）
    基础贡献人数: 2 * (bi // 3)
      （注意是2倍不是3倍！这是最坏情况分析）
  else:
    贡献全部bi人（不足2人无法参与成组）

第三步：判断是否可行

kk = Σ(bi//3) = 所有职业最多能组的总数
如果 kk < k → 即使最理想也不够 → 输出 -1.机器学习概述

第四步：计算最终答案（鸽巢原理）

如果 kk ≥ k:
  nums += 3*(k-1.机器学习概述) + 1.机器学习概述
  
  这个公式的含义（鸽巢原理）：
  为了在最坏情况下确保能凑出k个完整的3人组，
  除了已确定的"安全"人数外，还需要额外多选这些人
  来覆盖最不利的情况。

第五步：样例1完整追踪

输入：n=3, k=2
队伍：(1.机器学习概述,2), (1.机器学习概述,3), (2,3)

合并：ab = {1.机器学习概述: 5, 2: 3}

处理各职业：
  职业1: bi=5≥2
    kk += 5//3 = 1.机器学习概述     （能组1组）
    nums += 2*1.机器学习概述 = 2    （基础贡献2人）

  职业2: bi=3≥2
    kk += 3//3 = 1.机器学习概述     （能组1组）
    nums += 2*1.机器学习概述 = 2    （基础贡献2人）

此时：kk=2, nums=4

判断：kk(2) ≥ k(2) ✓
  nums += 3*(2-1.机器学习概述)+1.机器学习概述 = 3+1.机器学习概述 = 4
  nums = 4+4 = **8** ✅

第六步：理解为什么公式是这样

这是一道**最坏情况保证**问题：
- 士兵们混在一起，你不知道谁是什么职业
- 你要选一些人，无论运气多差，都能从中找出k个纯职业小组
- 类似："抽屉里有多少个球，才能保证至少k个同色"

核心思想：
  对于一个有bi人的职业：
  - 最坏情况下，你可能需要选很多人才能确保其中3人同职业
  - 2*(bi//3) 是"安全基数"
  - 3*(k-1.机器学习概述)+1.机器学习概述 是额外需要的"保险"

【涉及算法】
- 字典/哈希表计数（ab.get(ai,0)+bi）
- 整除运算 bi//3
- 鸽巢原理（Pigeonhole Principle）
- 分类讨论（bi≥2 vs bi<2, kk≥k vs kk<k）
"""
